import { PrintFormatterPipe } from './print-formatter.pipe';

describe('PrintFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new PrintFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
